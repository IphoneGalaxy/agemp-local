{
  "name": "controle-de-emprestimos-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.js && node server.js",
    "build": "node build.js && mkdir -p dist && cp -r index.html assets js dist/ 2>/dev/null || true",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
